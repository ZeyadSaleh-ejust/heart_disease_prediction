this is heart disease prediction project


to run this project, first install the required libraries using the command below:

```bash
pip install -r requirements.txt
```

then run 
```bash
python app.py
```